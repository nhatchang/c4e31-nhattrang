1. Search and answer the questions:

- How to check a variable’s type?
To check a variable's type, we use "typeof"
> typeof "foo"
"string"
> typeof true
"boolean"
> typeof 42
"number"

- In what cases, you will get SyntaxError from the compiler telling you that some of your variables have invalid names? Can you give 3 different examples of invalid names?
You will get SyntaxError from the compiler when your variable names include: special characters (*, &, #,...); Space; variable names is duplicated (var, if, for)
eg: let nhat chang = hello (space)
    let bla*bla = abcde (special characters)
    let var = something (duplicated)

2. Write a program that calculates the area of a circle. The circle radius is entered by users.
let x = prompt("Enter Circle Radius");
console.log("Area=", x*x*3.14)

3. Write a program that converts Celsius (0C) into Fahrenheit (0F) 
let y = prompt("Enter temperature in Celsius?");
console.log(c,"(ᵒC)=", c*1.8+32,"(F)")

4. a square
fd(100)
rt(90)
fd(100)
rt(90)
fd(100)
rt(90)
fd(100)
rt(90)

an equilateral triangle
rt(30)
fd(100)
rt(120)
fd(100)
rt(120)
fd(100)

a circle
for (let i = 0; i < 360; i++) {
fd(1)
rt(1)
}

multi-circles
for (let i = 0; i < 20; i++) {
rt(30)
for (let i = 0; i < 360; i++) {
fd(1)
rt(1)
}
}
